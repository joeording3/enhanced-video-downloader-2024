# Enhanced Video Downloader server configuration (.env)
# Lines starting with '#' are comments. Values are read at process start.
#
# Core server settings (loaded into ServerConfig via environment):
# - SERVER_PORT: port the Flask server listens on (1024-65535).
# - DOWNLOAD_DIR: absolute path for downloads. Must be writable.
# - DEBUG_MODE: enable debug features (true/false).
# - ENABLE_HISTORY: enable download history persistence (true/false).
# - LOG_LEVEL: application log level (debug, info, warning, error, critical).
# - CONSOLE_LOG_LEVEL: console log verbosity (debug, info, warning, error, critical).
# - ALLOW_PLAYLISTS: permit playlist downloads (true/false).
# - LOG_FILE: path used by /logs endpoint when set.
#
SERVER_PORT=9090
DOWNLOAD_DIR=/Users/josephording/Downloads/untitled folder
DEBUG_MODE=true
ENABLE_HISTORY=true
LOG_LEVEL=info
CONSOLE_LOG_LEVEL=info
ALLOW_PLAYLISTS=true
LOG_FILE=/Users/josephording/myscripts/Enhanced Video Downloader/server_output.log

# Runtime startup defaults for CLI (used when flags are not provided):
# - EVD_GUNICORN: run with Gunicorn when true (production-style).
# - EVD_WORKERS: number of Gunicorn workers (integer).
# - EVD_VERBOSE: enable verbose console logging (similar to ).
EVD_GUNICORN=true
EVD_WORKERS=2
EVD_VERBOSE=true

# Notes:
# - YT_DLP_OPTIONS via env is not supported; configure yt-dlp options via the /api/config endpoint.
# - After changes, restart the server to apply.
